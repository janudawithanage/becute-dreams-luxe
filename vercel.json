{
  "version": 2,
  "framework": null,
  "buildCommand": "npx vite build && cp -r dist/server api/server",
  "outputDirectory": "dist/client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/ssr"
    }
  ]
}
